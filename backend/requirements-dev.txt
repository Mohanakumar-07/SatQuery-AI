# SatQuery AI — backend development / test dependencies
# Install:  pip install -r backend/requirements.txt -r backend/requirements-dev.txt

pytest>=8.0
pytest-cov>=5.0
httpx>=0.27                  # required by fastapi.testclient.TestClient
ruff>=0.4
mypy>=1.10
types-requests
